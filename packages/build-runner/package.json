{
  "name": "@gadgets/build-runner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "echo 'run pnpm dev-server in the root directory instead' >&2 && exit 1",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "types:check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.119.0",
    "@cloudflare/workers-types": "^5.20260804.1"
  }
}
